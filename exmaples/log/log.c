// Include the standard input/output library for C.
#include <stdio.h>  

// Define a function called "lg" that takes a string as an argument.
int lg(char *message)  
{
    // Print the received string argument to the console.
    printf("%s\n", message);  
    // Return an integer value of 0.
    return 0;  
}